{
  "name": "@boundlesseth/monorepo",
  "repository": "https://github.com/boundlesseth/monorepo",
  "license": "GPL-3.0",
  "private": true,
  "workspaces": {
    "packages": [
      "contracts",
      "vue-app"
    ]
  },
  "scripts": {
    "compile": "yarn workspace contracts hardhat compile"
  }
}
